- [x] Add PPAs `sudo apt-add-repository -y`
    ppa:klaus-vormweg/awesome
    ppa:ansible/ansible
- [x] Add apt repositories
    wget -qO - https://wavebox.io/dl/client/repo/archive.key | sudo apt-key add -
    echo "deb https://wavebox.io/dl/client/repo/ x86_64/" | sudo tee --append /etc/apt/sources.list.d/repo.list
    wget -qO - http://linux-packages.resilio.com/resilio-sync/key.asc | sudo apt-key add -
    echo "deb http://linux-packages.resilio.com/resilio-sync/deb resilio-sync non-free" | sudo tee /etc/apt/sources.list.d/resilio-sync.list
- [x] Update apt: `sudo apt update`
- [x] Install packages:
    sudo apt install git vim vim-gtk scudcloud pidgin chromium-browser chromium-codecs-ffmpeg-extra silversearcher-ag libreoffice gimp inkscape imagemagick ghostscript google-drive-ocamlfuse ttf-mscorefonts-installer wavebox fonts-inconsolata screen ssh apt-transport-https ca-certificates curl software-properties-common xclip s3cmd resilio-sync awesome smartmontools ansible zfs-dkms zfsutils-linux zfs-initramfs
    - [x] checkout and link dotfiles
- [ ] setup wavebox
- [ ] setup chromium
- [ ] setup google drive:
    sudo groupadd fuse
    sudo usermod -a -G fuse aj
    google-drive-ocamlfuse
    mkdir ~/googledrive
    google-drive-ocamlfuse ~googledrive
    mount
- [x] generate ssh key
- [ ] add ssh key to github
- [ ] add ssh key to server configs
- [x] install flameshot
- [ ] add items to xfce autostart: xsession, flameshot_monitor.sh, wavebox
- [x] setup rvm
- [x] setup nvm
- [ ] add pulseaudio plugin to xfce dock
- [ ] set awesome to be default window manager
